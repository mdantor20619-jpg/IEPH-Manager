# 🛠 Troubleshooting Guide (বাংলায়)

### ১. পেজ রিফ্রেশ করলে ৪04 বা ৪MD আসে?
- **সমাধান:** নিশ্চিত করুন আপনার প্রজেক্টের রুট ফোল্ডারে `netlify.toml` এবং `public/_redirects` ফাইল দুটি আছে। এই ফাইলগুলো Netlify-কে বলে দেয় যে সব রিকোয়েস্ট `index.html`-এ পাঠাতে।

### ২. Google Login কাজ করছে না?
- **চেক ১:** Netlify ড্যাশবোর্ডে গিয়ে `Site Settings > Environment Variables` এ `REACT_APP_GOOGLE_CLIENT_ID` যোগ করেছেন কি?
- **চেক ২:** Google Cloud Console-এ 'Authorized JavaScript Origins'-এ আপনার নেটলিফাই ইউআরএল (https://your-app.netlify.app) যোগ করেছেন কি?

### ৩. বিল্ড ফেইল হলে (Build Fail):
- আপনার `package.json` এ `npm run build` কমান্ডটি ঠিক আছে কিনা দেখুন। 
- Vite প্রজেক্টে সাধারণত আউটপুট `dist` ফোল্ডারে যায়, তাই Netlify-তে Publish directory অবশ্যই `dist` হতে হবে।

### ৪. লগইন করার পর সাদা স্ক্রিন?
- ব্রাউজারের কনসোল (F12) চেক করুন। যদি "Invalid Client ID" দেখায়, তবে বুঝবেন এনভায়রনমেন্ট ভেরিয়েবল সেট করা হয়নি বা ভুল হয়েছে।