# 🚀 IEPH Manager Deployment Guide (বাংলায়)

এই গাইডটি অনুসরণ করে আপনি আপনার অ্যাপটি সম্পূর্ণ ফ্রিতে ইন্টারনেটে লাইভ করতে পারবেন।

### ১. GitHub Setup (কোড আপলোড)
১. একটি GitHub অ্যাকাউন্ট খুলুন (যদি না থাকে)।
২. আপনার কম্পিউটারে এই প্রজেক্টের ফোল্ডারে গিয়ে টার্মিনালে নিচের কমান্ডগুলো দিন:
   ```bash
   git init
   git add .
   git commit -m "Initial launch"
   git branch -M main
   git remote add origin [YOUR_GITHUB_REPO_URL]
   git push -u origin main
   ```

### ২. Google OAuth Setup (লগইন সিস্টেম সচল করা)
লগইন কাজ করার জন্য এটি সবচেয়ে গুরুত্বপূর্ণ ধাপ:
১. [Google Cloud Console](https://console.cloud.google.com/) এ যান।
২. একটি **New Project** তৈরি করুন (নাম দিন: IEPH-Manager)।
৩. **APIs & Services > OAuth consent screen** এ যান:
   - User Type: **External** দিন।
   - App Name: **IEPH Manager**।
   - Support Email: আপনার নিজের জিমেইল দিন।
   - Developer Contact Info: আপনার জিমেইল দিন।
   - এরপর 'Save and Continue' দিয়ে শেষ পর্যন্ত যান এবং 'Back to Dashboard' দিন।
   - **Publish App** বাটনে ক্লিক করে অ্যাপটি 'In Production' মোডে নিন।
৪. **Credentials > Create Credentials > OAuth client ID** এ ক্লিক করুন:
   - Application Type: **Web application**।
   - **Authorized JavaScript origins**: 
     - `http://localhost:5173` (লোকাল টেস্টের জন্য)
     - `https://your-app-name.netlify.app` (আপনার নেটলিফাই ইউআরএল)
   - 'Create' বাটনে ক্লিক করলে আপনি একটি **Client ID** পাবেন। এটি কপি করে রাখুন।

### ৩. Netlify Deployment (লাইভ করা)
১. [Netlify](https://www.netlify.com/) এ লগইন করুন।
২. **Add new site > Import from existing project** দিন।
৩. GitHub কানেক্ট করে আপনার রিপোজিটরি সিলেক্ট করুন।
৪. **Environment Variables** সেকশনে গিয়ে `Add Variable` এ ক্লিক করুন:
   - Key: `REACT_APP_GOOGLE_CLIENT_ID`
   - Value: [আপনার কপি করা Client ID টি এখানে দিন]
৫. **Deploy site** বাটনে ক্লিক করুন।

### ৪. টেস্টিং চেকলিস্ট
- [ ] অ্যাপটি ব্রাউজারে লোড হচ্ছে কিনা।
- [ ] Google Login বাটনে ক্লিক করলে পপআপ আসছে কিনা।
- [ ] স্টুডেন্ট ডাটা সেভ করার পর পেজ রিফ্রেশ করলে ডাটা থাকছে কিনা।
- [ ] মোবাইল ভিউ ঠিক আছে কিনা।
